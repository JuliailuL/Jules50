def main():
    # Distill main in a single line of code and then just pass in the number of...
    #...times you want it executed:
    meow(3)

# Give the function a parameter...e.g. 'n'
def meow(n):
    # Give me a range that is a variable length based on what n is
    for i in range(n)
        print("meow")

main()
